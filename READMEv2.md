what you should be careful from:
if the user puts cordinates that are in the same place as the 42 pattern, it should return an err
the seed should be able to take strings, not just int
the documentation in the code, and in the README should be as described in the subject
