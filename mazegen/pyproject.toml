[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mazegen"
version = "1.0.0"
description = "A procedural maze generator and solver."
authors = [
  { name="mboubaza" },
  { name="iel-fadi" }
]
requires-python = ">=3.10"